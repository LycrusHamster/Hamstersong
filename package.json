{
  "name": "hamstersong",
  "version": "1.0.0",
  "description": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "truffle migrate --reset --network development",
    "compile": "truffle compile --all",
    "debug": "npm run compile && npm run migrate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "1.1.0",
    "truffle": "^5.1.44"
  }
}
